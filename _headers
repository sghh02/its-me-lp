# Netlify/Cloudflare Headers Configuration
# For static hosting services that support _headers file

/*
  # Security headers
  # Note: Most common headers (X-Frame-Options, X-XSS-Protection, X-Content-Type-Options,
  # Referrer-Policy, Permissions-Policy) are defined in netlify.toml to avoid duplication.
  # Keep CSP and HSTS here as they are not set in netlify.toml.
  Content-Security-Policy: default-src 'self'; script-src 'self' 'unsafe-inline' 'unsafe-eval' https://www.googletagmanager.com https://www.google-analytics.com; style-src 'self' 'unsafe-inline' https://fonts.googleapis.com; font-src 'self' https://fonts.gstatic.com; img-src 'self' data: https:; connect-src 'self' https://www.google-analytics.com; frame-ancestors 'none'; base-uri 'self'; form-action 'self';
  Strict-Transport-Security: max-age=31536000; includeSubDomains; preload

# Cache Control is defined in netlify.toml to keep a single source of truth.
